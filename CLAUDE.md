# YouTube 数据采集工具 - 技术规格说明书

## 📋 项目概述

一个基于 Python 的 Windows 桌面应用程序，用于批量采集 YouTube 视频数据并导出为 Excel 文件。

### 核心功能
- 根据关键词搜索 YouTube 视频（最多1000条）
- 自动翻页获取所有搜索结果
- 提取视频详细数据（播放量、点赞、评论等）
- 智能提取 KOL 联系方式（通过 DeepSeek AI 分析）
- 多 API Key 管理与智能轮换
- 导出为 Excel 格式

### 目标用户
- 营销人员、数据分析师
- Windows 10/11 系统
- 非技术背景，需要开箱即用

---

## 🛠 技术栈

### 核心技术
- **语言**: Python 3.8+
- **UI 框架**: CustomTkinter（现代化界面）
- **API 集成**:
  - YouTube Data API v3
  - DeepSeek API
- **数据处理**: pandas
- **Excel 导出**: openpyxl
- **打包工具**: PyInstaller（文件夹模式）

### 依赖库
```
customtkinter
google-api-python-client
pandas
openpyxl
requests
```

---

## 📊 数据字段设计

### Excel 输出字段（按列顺序）

| 列 | 字段名 | 说明 | 数据源 | 是否可选显示 |
|---|---|---|---|---|
| A | keyword | 搜索关键词 | 用户输入 | 必显示 |
| B | KOL名称 | 频道名称 | YouTube API | 必显示 |
| C | 视频链接 | 完整URL（可点击） | YouTube API | 必显示 |
| D | 播放数量 | 数字格式 | YouTube API | 必显示 |
| E | 互动率 | (点赞+评论)/播放量 | 计算得出 | 必显示 |
| F | 联系方式 | AI提取 | DeepSeek分析 | 必显示 |
| G | 点赞数量 | 数字格式 | YouTube API | 必显示 |
| H | 评论数量 | 数字格式 | YouTube API | 必显示 |
| I | 视频标题 | 文本 | YouTube API | 必显示 |
| J | 发布日期 | YYYY-MM-DD | YouTube API | 必显示 |
| K | 视频时长 | MM:SS 格式 | YouTube API | 必显示 |
| L | 频道订阅数 | 数字格式 | YouTube API | 必显示 |
| M | 频道链接 | 完整URL | YouTube API | **可选显示** |
| N | 视频描述 | 前200字 | YouTube API | **可选显示** |
| O | 视频语言 | 语言代码 | YouTube API | 必显示 |

### 字段获取策略
- **后台获取**: 频道链接(M)、视频描述(N) 始终在后台获取并缓存
- **前端显示**: 用户可在界面勾选是否在 Excel 中显示这两个字段
- **默认设置**: 默认不显示 M、N 两列

---

## 🔑 API 管理策略

### 1. API Key 批量管理

#### 输入方式
**方式A: 单个添加**
```
- 输入 API Key
- 输入备注名称（如 "主账号1"）
- 点击 "测试并添加"
```

**方式B: 批量导入**
```
支持格式: CSV / TXT / JSON

CSV格式示例:
AIzaSyXXXXXXXXXXXX,主账号1
AIzaSyYYYYYYYYYYYY,备用账号2

TXT格式示例（一行一个）:
AIzaSyXXXXXXXXXXXX
AIzaSyYYYYYYYYYYYY

JSON格式示例:
[
  {"key": "AIzaSyXXXXXXXXXXXX", "name": "主账号1"},
  {"key": "AIzaSyYYYYYYYYYYYY", "name": "备用账号2"}
]
```

**方式C: 引导创建**
```
- 点击 "如何获取 API Key"
- 打开浏览器到 Google Cloud Console
- 显示详细图文教程
```

#### 自动验证功能
```python
批量验证流程:
1. 并发测试所有 Key 是否有效
2. 调用 YouTube API 进行简单测试
3. 初始化配额为 10,000 单位
4. 显示验证结果:
   ✓ 有效 (10000/10000)
   ✗ 无效 (Invalid API Key)
```

### 2. API Key 轮换策略（智能切换 + 平均分配）

**策略组合: A + B**

#### 顺序轮换（策略A）
```python
基本规则:
- Key1 配额用到 9,500 → 切换到 Key2
- Key2 配额用到 9,500 → 切换到 Key3
- 所有 Key 耗尽 → 提示用户

目的: 简单可靠，避免配额超限
```

#### 引导创建（策略B）
```python
辅助功能:
- 提供详细的 API Key 创建教程
- 一键打开 Google Cloud Console
- 分步骤截图说明
- 常见问题解答

目的: 降低用户获取 API Key 的门槛
```

### 3. 配额监控（本地计算 + 错误检测）

```python
配额管理:
- 启动时: 每个 Key 配额重置为 10,000
- 调用后: 根据 API 类型扣减配额
  • search.list: -100 单位
  • videos.list: -1 单位
  • channels.list: -1 单位
- 每天 UTC 0点: 自动重置配额
- 收到 403 quotaExceeded: 立即标记 Key 已耗尽

配额计算示例（1000个视频）:
- 搜索翻页: 20次 × 100 = 2,000 单位
- 视频详情: 1000次 × 1 = 1,000 单位
- 频道信息: 500次 × 1 = 500 单位
总计: 3,500 单位（约35%配额）
```

---

## 🚀 性能与安全策略

### 1. 请求频率控制（防止封禁）

```python
安全策略（15-20分钟完成1000个视频）:

并发控制:
- 搜索请求: 单线程，间隔 500ms
- 视频详情: 8-10 线程并发，每个请求间隔 100ms
- 频道信息: 5 线程并发，间隔 150ms
- DeepSeek 分析: 3-5 线程并发

重试策略:
- 收到 429 (Too Many Requests) → 等待 5秒重试
- 连续失败 3次 → 切换 API Key
- 所有 Key 都 429 → 暂停 30秒后继续
- 网络超时 → 自动重试，最多 3次

目标: 安全优先，避免被 YouTube 封禁
```

### 2. 取消机制

```python
取消操作:
1. 点击 "取消" 按钮
2. 立即终止所有正在进行的 API 请求
3. 丢弃所有已获取的数据（不保存）
4. 保留频道级联系方式缓存（避免重复调用 DeepSeek）
5. 重置进度条和状态
6. 返回就绪状态

实现方式:
- 使用线程事件 (threading.Event) 控制
- 所有工作线程检查取消标志
- 收到取消信号立即退出
```

### 3. 搜索排序方式

```python
用户可选排序（下拉菜单）:
- relevance: 相关性（默认）
- date: 发布日期
- viewCount: 观看次数
- rating: 评分

保存用户上次选择
```

---

## 🤖 DeepSeek 联系方式提取

### 1. 频道级缓存策略

```python
缓存机制:
- 按频道 ID 缓存联系方式
- 同一个 KOL 只调用一次 DeepSeek
- 缓存保存到本地 JSON 文件
- 缓存有效期: 180天
- 提供 "清除缓存" 按钮手动清理

缓存结构:
{
  "channel_id_1": {
    "contact_info": "邮箱: xxx@gmail.com; 微信: xxxxx",
    "timestamp": "2025-01-23T10:30:00",
    "channel_name": "某某KOL"
  }
}

优势:
- 大幅减少 API 调用次数（节省成本）
- 提升处理速度
- 同一 KOL 的多个视频无需重复分析
```

### 2. DeepSeek 提示词（方案B增强版）

```python
系统提示词:
"""
你是一个专业的联系方式提取助手。
请从以下 YouTube 频道简介中提取所有联系方式，包括但不限于：
- 邮箱地址
- 微信号
- Telegram 账号
- WhatsApp
- Instagram
- Twitter/X
- 官方网站地址

提取规则：
1. 只提取明确的联系方式，不要推测
2. 如果没有找到任何联系方式，返回 "无"
3. 多个联系方式用分号分隔
4. 返回格式：邮箱: xxx@xxx.com; 微信: xxx; 官网: https://xxx.com
5. 官方网站必须是完整的 URL 格式

简介内容：
{channel_description}

请直接返回联系方式，不要其他解释。
"""

示例输出:
邮箱: contact@example.com; 微信: example123; 官网: https://example.com

或

无
```

---

## ⚠️ 错误处理策略

### 1. 异常数据处理

| 异常情况 | 处理方式 |
|---|---|
| 重复视频 | 自动去重（按视频ID） |
| 私有/删除的视频 | 跳过，记录到日志 |
| 播放量为 0 | 正常记录，互动率显示 0.00% |
| 评论已关闭 | 评论数记为 0 |
| 频道简介为空 | 联系方式记为 "无" |
| 地区限制视频 | 跳过，记录到日志 |
| API 请求失败 | 重试3次，仍失败则跳过 |
| DeepSeek 分析失败 | 联系方式记为 "获取失败" |

### 2. 日志系统

```python
日志文件:
位置: logs/YYYY-MM-DD.log
格式: 时间戳 | 级别 | 消息

日志内容:
[2025-01-23 10:30:15] INFO | 开始搜索关键词: iPhone15
[2025-01-23 10:30:20] INFO | 找到 1000 个视频
[2025-01-23 10:30:25] WARNING | 视频 dQw4w9WgXcQ 无法获取详情（已删除）
[2025-01-23 10:30:30] ERROR | API Key #1 配额耗尽，切换到 Key #2
[2025-01-23 10:50:00] INFO | 成功获取 956 个视频，跳过 44 个
[2025-01-23 10:50:05] INFO | 导出 Excel: iPhone15_20250123.xlsx

界面显示:
- 简单统计: "成功: 956条, 失败: 44条"
- 提供 "查看日志" 按钮打开日志文件
```

---

## 💾 配置管理

### 1. 配置文件

```python
配置目录:
Windows: C:\Users\{用户名}\AppData\Local\YouTubeSearchTool\

文件结构:
YouTubeSearchTool/
├── config.json          # 用户配置
├── cache.json           # 频道联系方式缓存
└── logs/                # 日志文件夹
    ├── 2025-01-23.log
    └── 2025-01-24.log

config.json 结构:
{
  "youtube_api_keys": [
    {
      "key": "AIzaSyXXXXXXXXXXXX",
      "name": "主账号1",
      "quota_used": 3500,
      "quota_total": 10000,
      "last_reset": "2025-01-23T00:00:00"
    }
  ],
  "deepseek_api_key": "sk-XXXXXXXXXXXX",
  "last_search_keyword": "iPhone15",
  "last_max_results": 1000,
  "last_sort_order": "relevance",
  "show_channel_link": false,
  "show_video_description": false
}

cache.json 结构:
{
  "channels": {
    "UC_x5XG1OV2P6uZZ5FSM9Ttw": {
      "contact_info": "邮箱: xxx@gmail.com",
      "timestamp": "2025-01-23T10:30:00",
      "channel_name": "Google Developers"
    }
  }
}
```

### 2. 缓存清理策略

```python
自动清理:
- 启动时检查缓存时间戳
- 删除 180天 前的缓存记录
- 日志文件保留最近 30天

手动清理:
- 界面提供 "清除缓存" 按钮
- 显示缓存数量和占用空间
- 确认后清空 cache.json
```

---

## 🎨 用户界面设计（CustomTkinter）

### 1. 主窗口布局

```
┌──────────────────────────────────────────────────────┐
│  YouTube 数据采集工具                          v1.0.0  │
├──────────────────────────────────────────────────────┤
│  [设置] [API管理] [查看日志] [关于]                    │
├──────────────────────────────────────────────────────┤
│  搜索配置                                              │
│  ┌──────────────────────────────────────────────┐    │
│  │ 关键词: [_________________________]          │    │
│  │                                              │    │
│  │ 最大结果数: [1000 ▼]  (50/100/200/500/1000) │    │
│  │                                              │    │
│  │ 排序方式: [相关性 ▼]  (相关性/日期/观看数/评分)│    │
│  │                                              │    │
│  │ 可选字段:                                     │    │
│  │ ☐ 显示频道链接  ☐ 显示视频描述               │    │
│  │                                              │    │
│  │            [开始搜索]                         │    │
│  └──────────────────────────────────────────────┘    │
├──────────────────────────────────────────────────────┤
│  进度信息                                              │
│  ┌──────────────────────────────────────────────┐    │
│  │ 进度: ████████████░░░░░░░░░░ 45% (450/1000) │    │
│  │                                              │    │
│  │ 状态: 正在获取视频详情... (API Key: 主账号1)  │    │
│  │                                              │    │
│  │ 预计剩余时间: 8分钟                           │    │
│  │                                              │    │
│  │                              [取消]          │    │
│  └──────────────────────────────────────────────┘    │
├──────────────────────────────────────────────────────┤
│  数据预览（前10条）                                    │
│  ┌──────────────────────────────────────────────┐    │
│  │ KOL名称    | 播放量  | 互动率 | 联系方式      │    │
│  │ ───────────┼─────────┼────────┼──────────    │    │
│  │ 示例频道1  | 1.2M    | 3.45%  | 邮箱:xxx@... │    │
│  │ 示例频道2  | 850K    | 5.67%  | 微信:xxx     │    │
│  │ ...                                          │    │
│  └──────────────────────────────────────────────┘    │
├──────────────────────────────────────────────────────┤
│  [导出Excel]  [清空数据]             成功: 956 失败: 44│
└──────────────────────────────────────────────────────┘
```

### 2. API 管理窗口

```
┌──────────────────────────────────────────────────────┐
│  API Key 管理                                          │
├──────────────────────────────────────────────────────┤
│  YouTube API Keys                                      │
│  ┌──────────────────────────────────────────────┐    │
│  │ ☑ AIza...abc (9234/10000) [主账号1]   [编辑] │    │
│  │ ☑ AIza...def (156/10000)  [备用账号2] [编辑] │    │
│  │ ☐ AIza...ghi (10000/10000)[已耗尽]    [编辑] │    │
│  │                                              │    │
│  │ [单个添加] [批量导入] [如何获取API Key]       │    │
│  └──────────────────────────────────────────────┘    │
│                                                        │
│  DeepSeek API Key                                      │
│  ┌──────────────────────────────────────────────┐    │
│  │ [sk-XXXXXXXXXXXXXXXXXXXX]          [测试]    │    │
│  └──────────────────────────────────────────────┘    │
│                                                        │
│  缓存管理                                              │
│  ┌──────────────────────────────────────────────┐    │
│  │ 当前缓存: 256 个频道 (1.2 MB)                │    │
│  │ 最后清理: 2025-01-20                         │    │
│  │                              [清除缓存]       │    │
│  └──────────────────────────────────────────────┘    │
│                                                        │
│              [保存]          [取消]                    │
└──────────────────────────────────────────────────────┘
```

### 3. 首次启动引导（多步骤向导）

```
步骤 1/3: 欢迎
┌──────────────────────────────────────────┐
│  欢迎使用 YouTube 数据采集工具            │
│                                          │
│  本工具可以帮助您：                       │
│  • 批量采集 YouTube 视频数据             │
│  • 自动提取 KOL 联系方式                 │
│  • 导出为 Excel 格式                     │
│                                          │
│  在开始之前，需要配置 API Key             │
│                                          │
│                    [下一步]              │
└──────────────────────────────────────────┘

步骤 2/3: YouTube API 配置
┌──────────────────────────────────────────┐
│  配置 YouTube API Key                     │
│                                          │
│  [如何获取 API Key（点击查看教程）]       │
│                                          │
│  输入 API Key:                            │
│  [_________________________________]     │
│                                          │
│  备注名称（可选）:                        │
│  [_________________________________]     │
│                                          │
│              [测试] [添加更多]            │
│                                          │
│          [上一步]    [下一步]            │
└──────────────────────────────────────────┘

步骤 3/3: DeepSeek API 配置
┌──────────────────────────────────────────┐
│  配置 DeepSeek API Key                    │
│                                          │
│  用于智能提取 KOL 联系方式                │
│                                          │
│  输入 API Key:                            │
│  [_________________________________]     │
│                                          │
│                          [测试]          │
│                                          │
│  [如何获取 DeepSeek API Key]             │
│                                          │
│          [上一步]    [完成]              │
└──────────────────────────────────────────┘
```

---

## 📦 打包与发布

### 1. 打包配置

```python
打包工具: PyInstaller（文件夹模式）

打包命令:
pyinstaller --name="YouTube数据采集工具" \
            --windowed \
            --icon=icon.ico \
            --add-data="assets;assets" \
            main.py

输出:
dist/YouTube数据采集工具/
├── YouTube数据采集工具.exe
├── 依赖文件...
└── assets/

预计体积: 50-80 MB
启动速度: 2-3秒
```

### 2. 文件命名规则

```python
Excel 导出自动命名:
格式: {关键词}_{日期时间}.xlsx
示例: iPhone15_20250123_143052.xlsx

文件保存对话框:
- 默认文件名已填写
- 用户可以修改
- 默认保存位置: 用户的"文档"文件夹
```

---

## 📁 项目文件结构

```
youtube-search-system/
├── src/
│   ├── api/
│   │   ├── __init__.py
│   │   ├── youtube_api.py          # YouTube API 封装
│   │   │   ├── search_videos()     # 搜索视频
│   │   │   ├── get_video_details() # 获取视频详情
│   │   │   ├── get_channel_info()  # 获取频道信息
│   │   │   └── APIKeyManager()     # API Key 管理类
│   │   └── deepseek_api.py         # DeepSeek API 封装
│   │       └── extract_contact()   # 提取联系方式
│   │
│   ├── core/
│   │   ├── __init__.py
│   │   ├── data_collector.py       # 数据采集核心逻辑
│   │   ├── cache_manager.py        # 缓存管理
│   │   └── config_manager.py       # 配置管理
│   │
│   ├── gui/
│   │   ├── __init__.py
│   │   ├── main_window.py          # 主窗口
│   │   ├── api_manager_window.py   # API 管理窗口
│   │   ├── setup_wizard.py         # 首次启动向导
│   │   └── components/             # 可复用组件
│   │       ├── progress_bar.py
│   │       └── data_table.py
│   │
│   ├── utils/
│   │   ├── __init__.py
│   │   ├── excel_exporter.py       # Excel 导出
│   │   ├── logger.py               # 日志系统
│   │   └── helpers.py              # 辅助函数
│   │
│   └── main.py                     # 程序入口
│
├── assets/
│   ├── icon.ico                    # 应用图标
│   └── tutorial/                   # API Key 获取教程图片
│
├── tests/                          # 单元测试
│   ├── test_youtube_api.py
│   └── test_deepseek_api.py
│
├── requirements.txt                # Python 依赖
├── README.md                       # 用户说明文档
├── CLAUDE.md                       # 本文档（技术规格）
└── .gitignore
```

---

## 🔒 安全与隐私

### 数据安全
- API Key 存储在本地配置文件（**不加密**，根据用户需求）
- 不上传任何数据到第三方服务器
- 所有数据处理均在本地完成

### 隐私保护
- 不收集用户任何个人信息
- 日志文件仅存储在本地
- 用户可随时删除所有配置和缓存

---

## 📝 开发清单

### 阶段1: 核心功能开发
- [ ] YouTube API 封装
- [ ] DeepSeek API 封装
- [ ] 数据采集逻辑
- [ ] 频道联系方式缓存
- [ ] Excel 导出功能

### 阶段2: API Key 管理
- [ ] 单个添加功能
- [ ] 批量导入（CSV/TXT/JSON）
- [ ] API Key 验证
- [ ] 智能轮换逻辑
- [ ] 配额监控

### 阶段3: 用户界面
- [ ] 主窗口设计
- [ ] API 管理窗口
- [ ] 首次启动向导
- [ ] 进度显示
- [ ] 取消机制

### 阶段4: 优化与测试
- [ ] 日志系统
- [ ] 错误处理
- [ ] 性能优化
- [ ] 单元测试
- [ ] 用户测试

### 阶段5: 打包与发布
- [ ] PyInstaller 打包
- [ ] 图标和资源
- [ ] 用户文档
- [ ] 安装测试

---

## 📞 技术支持

### API 配额限制
- YouTube API: 10,000 单位/天/Key
- 1000个视频消耗约 3,500 单位
- 建议配置 3+ 个 API Key

### DeepSeek API 成本
- 按 Token 计费
- 预估: 1000个频道约 0.5-1 元（需根据实际 API 定价）
- 频道级缓存大幅减少成本

---

## 🎯 成功指标

- ✅ 15-20分钟完成 1000个视频采集
- ✅ 准确率 > 95%（成功获取视频数据）
- ✅ 联系方式提取准确率 > 80%
- ✅ 打包后体积 < 100MB
- ✅ 用户界面友好，非技术人员可操作
- ✅ 稳定运行，不被 YouTube 封禁

---

## 📚 参考文档

- [YouTube Data API v3 官方文档](https://developers.google.com/youtube/v3)
- [CustomTkinter 文档](https://customtkinter.tomschimansky.com/)
- [DeepSeek API 文档](https://platform.deepseek.com/docs)
- [openpyxl 文档](https://openpyxl.readthedocs.io/)

---

**文档版本**: 1.0
**最后更新**: 2025-01-23
**维护者**: Claude AI
